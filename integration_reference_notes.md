# 参照プロジェクト統合調査メモ

## 参照元

| 統合先 | 参照タスクID | 参照名 |
|---|---|---|
| `/atlas/` | `JRrZ1NeQ5oDdzBrwPtaIXk` | 選手データ |
| `/fieldline/` | `EjJD7DRruwttm1AJiRPoex` | NFL2チーム成績比較 |

## ATLAS（選手データ）で確認した対象範囲

参照会話では、選手の検索・選択後に、名前、顔写真、ポジション、所属チームを即時表示し、年齢、生年月日、身長、体重、出身校、ドラフト情報、キャリア、受賞歴を段階的に表示する設計が確認できた。対象例はBrady Boyd（`00-0041205`）である。キャリアと受賞歴は遅延ロードされ、受賞歴がない選手では該当セクションを表示しない。

成果物一覧には、歴史的ロスター索引、キャリア所属チーム索引、過去スタッツのソース調査、退役選手カバレッジ監査、段階読み込みの性能検証に関する記録がある。統合時は、既存の現行ロスターと将来の選手プロフィールデータを分離し、プロフィール先行表示・詳細後続取得の方針を引き継ぐ。

## FIELDLINE（2チーム成績比較）で確認した対象範囲

参照会話と成果物から、任意の2チームを選択してシーズン・週を指定し、チーム別の主要指標、値、リーグ順位（`#`表記）を左右比較する機能が確認できた。チーム選択の端末保存、比較URL、共有導線、複数週・バイウィーク対応、公式NFLデータの監査、モバイルレイアウト・読み込み性能の検証が実装対象に含まれる。

比較の表示では「League Rank」の冗長なラベルを出さず、値の横に順位を`#順位`で表示する仕様が確認できた。成果物には比較URL、週選択、利用可能週、指標順序、チーム選択差分、シーズン更新、公式データ監査に関するテスト・検証記録がある。

## 統合設計上の原則

1. `FAN/HUB`の既存ボーンホワイト、インクネイビー、エンドゾーンオレンジとモバイル優先レイアウトを引き継ぐ。
2. ATLASは選手の検索・プロフィール・キャリア表示を、FIELDLINEは2チーム・週・シーズン選択と比較表を主要体験とする。
3. 現行サイトの右上メニューおよびモバイルメニューから`/atlas/`と`/fieldline/`へ到達できるようにする。
4. データは公式NFLデータを優先し、取得できない項目は推測で補完しない。

## 元アプリそのものを接続するための調査記録（2026-08-18）

参照タスクの保存済み画面・ルーター・データファイルは取得可能であり、元の機能が現行の簡易版より広いことを確認した。一方、FIELDLINEの参照プロジェクトIDから導出した既定公開URLは404であり、旧アプリを単純な外部リンク先として利用できる公開状態ではない。そのため、同じ機能を縮小せずに再利用するには、保存済みの元実装とデータモデルを現行プロジェクトへ取り込む方式を検討する必要がある。

その後、参照会話に記録されている2つの既存プレビューURLがいずれも現時点で稼働していることを実画面で確認した。ATLASは `https://3000-ituqp183l2vhyriklj7k9-6f5ac53f.sg1.manus.computer/`、FIELDLINEは `https://3000-im4jq1epwl9r74izjlp1y-9b5a11d5.sg1.manus.computer/` である。両者は元の検索・詳細表示、年・チーム・Week範囲の比較画面をそれぞれ保持している。ただし、プレビューURLは永続的な公開先としては扱わず、同じプロジェクトの正式な公開URLを確認できない場合は、元の実装を現行プロジェクトへ移植する必要がある。

2026-08-18に正式公開URLを実画面で確認した。ATLASは `https://nflplayeratl-tus9mrqw.manus.space/`、FIELDLINEは `https://nflteamstats-4q87cnse.manus.space/` であり、両者とも元のアプリ画面を返した。以後はプレビューURLではなく、この2つの正式公開URLを埋め込み先として使用する。

三本線メニュー追加後の開発プレビューでは、正式ATLAS URLをブラウザ単体で開くと元アプリが正常に表示された一方、FAN/HUB内のiframeはLOADING ATLAS表示を維持した。外部公開URL側の一時的な埋め込み制限またはロードイベント未完了を切り分ける必要がある。メニュー自体はロード中オーバーレイの上に表示された。

再確認では、iframeのロードイベントは完了したが、フレーム領域がブラウザのエラー画面に置き換わった。したがって、ローディング実装ではなく正式公開URLを開発プレビューからiframeとして利用する際の外部フレーム表示が原因である。元URLを単体で開くと正常表示されるため、恒久URLへの直接遷移は有効である。

2026-08-18の確認時点で `https://nfl-fanhub-japan.com/atlas/` は404を返した。これは現在のカスタムドメインが、ATLAS・FIELDLINE統合を含む最新チェックポイントではなく、以前の公開版を配信しているためである。最新チェックポイントの公開後に、実公開ドメイン上で埋め込み挙動を再検証する必要がある。

ATLAS・FIELDLINE共通の三本線メニューは、FIELDLINEの実画面で開閉を確認した。メニュー内にはHOMEとATLASへのリンク、現在地を示す非リンクのFIELDLINE項目が表示される。ローディングや埋め込み失敗時にもメニューは操作可能である。

正式FIELDLINE URLを単体で再確認すると元アプリは正常に表示されたが、その直後に開発プレビュー内の `/fieldline/` を開くとLOADING FIELDLINE表示を維持した。外部アプリの単体公開状態は正常であり、問題はFAN/HUBの開発プレビューからのiframe読み込みに限定される。

2026-08-18のATLAS正式公開URLの単体計測では、DOMContentLoadedは約6.8秒、load完了も約6.8秒だった。転送量は約167KB・17リソースであり、主な転送物はJavaScript約144KB、CSS約21KBだった。元アプリ本体の初期ナビゲーションが主な待機要因であり、FAN/HUB側では接続の事前確立と即時の操作可能状態を優先する。

同時点のFIELDLINE正式公開URLの単体計測では、DOMContentLoadedは約2.7秒、load完了も約2.7秒だった。転送量は約3KB・42リソースで、比較データAPIが約0.9〜1.0秒を占めた。ATLASの初回表示がより遅いため、両アプリ共通で接続の事前確立を行い、ATLASについては元プロジェクト側のJavaScript分割・初期データ取得の遅延化を次の優先候補とする。

FAN/HUB側では、両正式公開URLへのDNS解決・接続確立をHTMLヘッドから開始するよう変更した。読み込み中オーバーレイはpointer-eventsを無効化して三本線メニューを常に操作可能とし、iframe完了時は200msのフェードで解除する。390px幅のATLAS・FIELDLINEで、ローディング表示とメニューの同時表示を確認した。

2026-08-19に公開済みFAN/HUBの`/atlas/`を再確認すると、LOADING ATLAS表示のまま元アプリがフレーム内に描画されなかった。一方、ATLAS正式公開URLを単体で開くと検索画面が正常に表示された。HTTP応答もATLAS・FIELDLINEとも200で、`X-Frame-Options`および`Content-Security-Policy`のフレーム制限ヘッダーは応答ヘッダーに見当たらなかった。原因は元アプリそのものの停止ではなく、クロスオリジンiframeの読み込み完了・描画が公開FAN/HUB上で保証されない構成にある。

同日に`/fieldline/`でもLOADING FIELDLINE表示のままフレーム内容が描画されないことを確認した。FIELDLINE正式公開URLを単体で開くと、2025 Seasonの2チーム比較画面は正常に操作できた。両アプリとも同じ挙動であるため、公開FAN/HUBでのクロスオリジンiframe依存を解消し、正式URLへ直接遷移する導線に切り替える必要がある。

復旧後、FAN/HUB開発ルートの`/atlas/`はATLAS正式公開URLへ、`/fieldline/`はFIELDLINE正式公開URLへ自動的に直接遷移し、両方の元アプリ画面が描画されることを実機で確認した。`replace`遷移を使うため、ブラウザの戻る操作で壊れた埋め込み画面に戻ることはない。フレーム内メニューはクロスオリジン制約のため廃止し、FAN/HUBへはブラウザの戻る操作または元アプリのURLから戻る運用とする。

## ネイティブATLAS移植のデータ設計メモ（2026-08-19）

元ATLASは、NFLverseの公開リリース（`https://github.com/nflverse/nflverse-data/releases/download`）から`players.csv`、年度別`roster_{season}.csv`、週別ロスター、年度別選手成績を取得し、ESPNの公開NFL teams API、athlete API、awards APIを補完データとして使用する。検索はNFLverseの全選手マスターと直近ロスターを統合して、現役優先・名前前方一致優先で最大16件を返す。プロフィールはNFLverseで氏名・ポジション・背番号・生年月日・体格・大学・ドラフトを構成し、ESPNプロフィール画像を補完する。

キャリアはNFLverse週別ロスターを年度単位で集約し、連続年度と同じチーム構成を期間に畳み込む。元ATLASでは1999年以降の歴史ロスター索引を`/manus-storage/nfl-career-team-index_a331c990.json`、現役契約を`/manus-storage/nfl-active-contracts_9489346f.json`として保持し、Hall of FameはWikipedia API（`https://en.wikipedia.org/w/api.php?action=parse&page=List_of_Pro_Football_Hall_of_Fame_inductees&prop=wikitext&format=json&origin=*`）を週次キャッシュする。検索結果の先頭2件のプロフィール、プロフィール後の成績・契約、キャリア確認後の受賞歴を段階的にプリフェッチし、遅いデータで初期表示を阻害しない設計である。

2026-08-19のネイティブATLAS第1段階では、`/atlas/`が外部URLへ遷移せず、FAN/HUB内に「NFL PLAYER DIRECTORY」と名前検索入力（プレースホルダー「選手名を入力（2文字以上）」）を描画することをDOMで確認した。スクリーンショット環境では白いキャンバスとして描画されることがあったため、以後の検証ではDOMテキスト、検索API応答、モバイル画面確認を併用する。

同日、ネイティブATLASの名前検索へ`Mahomes`を入力し、NFLverse選手索引からPatrick Mahomes（QB、現役、KC、#15）が検索結果として返ることを実機で確認した。検索画面、FAN/HUBの三本線メニュー、チームカラーアクセントは同一ページ内で描画され、外部アプリへのリダイレクトは発生していない。

同日、Patrick MahomesのネイティブプロフィールでCAREER・AWARDS・STATS・CONTRACTの4タブが描画され、初期CAREERタブはNFLverse年度別ロスターからKansas City Chiefs（2017—2026、KC）を実データとして返すことを確認した。キャリア詳細は選手プロフィールの後に読み込まれ、検索の初期表示を阻害しない。

AWARDSタブはESPN公開プロフィールを後続リクエストとして取得する。Patrick Mahomesでは同APIが受賞項目を返さなかったため、「公開受賞歴は確認できませんでした。」という空状態を表示し、画面全体や他の詳細タブをブロックしないことを確認した。

シーズン成績タブは、選択直後に「シーズン成績を読み込んでいます」と非遮断のローディング表示を出し、CAREER・AWARDS・CONTRACTのタブ操作を維持する。開発中のHMR後も4タブとキャリア実データが再描画されることを確認した。

Patrick MahomesのSTATSタブでは、2017年から2025年までのNFLverseレギュラーシーズン成績（GP、CMP%、PASS YDS、TD、INT）が実データとして表示された。CONTRACTタブはnflverse/OverTheCapの公開契約アーカイブを取得するが、現時点では配布URLが応答しないため「公開契約アーカイブを現在取得できません。」と明確に表示する。これはプロフィール、キャリア、成績表示に影響しない独立した空状態である。

元ATLASの検索画面は、薄いブルーグレー背景の中央配置レイアウトで、上部に小型の「A / NFL PLAYER Atlas」ワードマーク、中央に白い丸角の検索パネル、下部左右にデータ出所とcurrent roster年度を配置する。名前検索ではパネル内にCURRENT ROSTERの小見出しを置き、画像付きの1行検索結果カード（名前、ポジション、現役タグ、略称・背番号、右端シェブロン）を表示する。検索パネルは現行ネイティブATLASより小さく、画面上下に大きな余白を取る。

元ATLASの選手詳細は、アプリシェル内で最大幅約30remの単一カラムを取り、白〜薄青灰の背景を使う。ヘッダーは戻るリンクと右寄せのPLAYER CARDラベルのみ。選手カードはチームprimaryから深いネイビーへ落ちるグラデーション、右上に28px角のヘッドショット、上部に略称・背番号、下部に3列のAGE／HEIGHT／WEIGHTを持つ。詳細タブは丸角カードではなく下線付きの3項目（キャリア、シーズン成績、契約情報）。キャリア初期表示には淡いアンバーの受賞歴カードと、左に細いラインとオレンジの点を置くチーム遍歴タイムラインが表示される。Patrick Mahomesの受賞歴はESPNから6件（2018 NFL MVPなど）取得されている。
