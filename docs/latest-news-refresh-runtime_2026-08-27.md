# LATEST NEWS REFRESH 実行時確認

## 実画面の状態遷移

開発プレビューのMIAページでREFRESHをクリックした直後、ボタン表示が `UPDATING` へ変化し、二重実行できない状態になったことを確認した。操作中も既存のニュース5件を保持するため、ユーザーは空白領域を見ることがない。

## 操作の範囲

この操作は対象チームのチーム公式RSSとNFL公式負傷情報をサーバー側で再取得し、検証済み項目を保存してから画面のフィードを再取得する。PFT・CBSはこの手動更新の対象外であり、既存キャッシュを継続表示する。
