# LATEST NEWS 公開時刻表示メモ

各ニュースカードでは、情報源バッジ・見出し・任意のRSS概要の下に `PUBLISHED · M/D HH:MM JST` を常時表示する。公開日時は保存済みの `published_at` を `Asia/Tokyo` で変換し、OFFICIAL、PFT、CBSで同じ表記を使う。

2026-08-18の390px幅BUF画面で、各カードに公開時刻が表示され、情報源・記事本文リンク・公開時刻が同時に判別できることを確認した。
